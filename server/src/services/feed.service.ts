// export const cleanFeedPayload = (payload: any) => {
//   console.log(payload.body);

//   const action=payload.action;
//   const actionUrl=payload.body.keys[1].url;
//   const url=payload.body.url;
//   const senderId=payload.body.sender.id
//   const sender=payload.body.sender.login
// };
